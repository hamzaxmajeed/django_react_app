# Django and React Task Tracker App

A Task Manager application created in React as the client-side, handling UI through requests to Django's backend server-side.

## Setup

Ensure you have python 3.6+ installed.

```bash
pip install -r requirements
```
Node.js - you can install node from nodejs.com and download it very easily, and you can check it if it was properly installed or not : node --version